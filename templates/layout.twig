<!DOCTYPE html>
<html lang="pl">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ page_title }} - {{ contact.short_name }}</title>

        <link rel="stylesheet" href="/css/style.css">

        {% block stylesheets %}{% endblock %}
    </head>

    <body>
        <header>
            <nav class="navbar">
                <div class="container">
                    <a href="/" class="logo">
                        <h1>{{ contact.short_name }}</h1>
                    </a>
                    <ul class="nav-menu">
                        <li><a href="/" class="{% if page_title == 'Strona główna' %}active{% endif %}">Strona
                                główna</a></li>
                        <li><a href="/galeria"
                                class="{% if page_title == 'Galeria zdjęć' %}active{% endif %}">Galeria</a></li>
                        <li><a href="/pracownicy"
                                class="{% if page_title == 'Pracownicy' %}active{% endif %}">Pracownicy</a></li>
                        <li><a href="/kontakt" class="{% if page_title == 'Kontakt' %}active{% endif %}">Kontakt</a>
                        </li>
                        <li><a href="/ui" class="{% if page_title == 'Szablony UI' %}active{% endif %}">Szablony UI</a>
                        </li>
                    </ul>
                </div>
            </nav>
        </header>

        <main>
            <div class="container">
                <h2 class="page-title">{{ page_title }}</h2>

                {% block content %}{% endblock %}
            </div>
        </main>

        <footer>
            <div class="container">
                <p>&copy; {{ site.copyright_year_start }}-{{ "now"|date("Y") }} {{ contact.name }}</p>
                <p>{{ contact.street }}, {{ contact.postal_code }} {{ contact.city }} | Tel: {{ contact.phone }}</p>

                {% if social %}
                <div class="social-links" style="margin-top: 1rem;">
                    {% if social.facebook %}<a href="{{ social.facebook }}" target="_blank" rel="noopener"
                        style="margin: 0 0.5rem; color: white;">Facebook</a>{% endif %}
                    {% if social.instagram %}<a href="{{ social.instagram }}" target="_blank" rel="noopener"
                        style="margin: 0 0.5rem; color: white;">Instagram</a>{% endif %}
                    {% if social.youtube %}<a href="{{ social.youtube }}" target="_blank" rel="noopener"
                        style="margin: 0 0.5rem; color: white;">YouTube</a>{% endif %}
                    {% if social.linkedin %}<a href="{{ social.linkedin }}" target="_blank" rel="noopener"
                        style="margin: 0 0.5rem; color: white;">LinkedIn</a>{% endif %}
                </div>
                {% endif %}
            </div>
        </footer>

        {% block scripts %}{% endblock %}
    </body>

</html>